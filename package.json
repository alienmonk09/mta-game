{
  "name": "mta-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Exército da Maruzza — jogo de marca do escritório Maruzza Teixeira (Phaser 4, modular)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^6.0.0",
    "vite": "^8.0.0",
    "vitest": "^4.0.0"
  }
}
