/**
 * Abstração de input (1 dedo / mouse / teclado). Implementação real na Fase 1:
 * expõe a posição horizontal normalizada (0..1) do líder do exército.
 */
export class InputManager {
  /* Fase 1: pointer/drag → posição alvo do líder */
}
